# GitHub Pages (Settings → Pages → branch: main, folder: /docs)
title: mrowser
description: D-pad web browser for Android TV with a native player for HLS, DASH and plain video files, plus live subtitle sync
exclude:
  - superpowers
  - marketing
